# Pin the Rust toolchain so all developers and CI see the same compiler.
# 1.77 is the minimum for wasm-bindgen 0.2.121; pick whatever current stable
# is at your install time — this file just makes the choice explicit.
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
